@page "/pdf-gosterici"
@layout VizitLink3DDuzen

<PageTitle>@_baslik</PageTitle>

<main class="orpay-page orpay-pdf-gosterici-sayfasi">
    <section class="orpay-panel orpay-pdf-gosterici-ust">
        <div>
            <MudText Typo="Typo.overline" Class="orpay-pdf-gosterici-etiket">@DilServisi.T("pdfGosterici.etiket", "BELGE GÖSTERİCİ")</MudText>
            <MudText Typo="Typo.h2" Class="orpay-pdf-gosterici-baslik">@_baslik</MudText>
        </div>
        <div class="orpay-pdf-gosterici-aksiyonlar">
            <MudButton Variant="Variant.Outlined" Color="Color.Primary" StartIcon="@Icons.Material.Filled.ArrowBack" OnClick="GeriDon">
                @DilServisi.T("ortak.geri", "Geri")
            </MudButton>
            @if (!string.IsNullOrWhiteSpace(_belgeUrl))
            {
                <MudButton Variant="Variant.Filled" Color="Color.Primary" StartIcon="@Icons.Material.Filled.Download" Href="@_belgeUrl" Target="_blank">
                    @DilServisi.T("pdfGosterici.indir", "PDF İndir")
                </MudButton>
            }
        </div>
    </section>

    <section class="orpay-panel orpay-pdf-gosterici-panel">
        @if (string.IsNullOrWhiteSpace(_belgeUrl))
        {
            <div class="orpay-pdf-gosterici-bos">
                <MudIcon Icon="@Icons.Material.Filled.PictureAsPdf" Size="Size.Large" />
                <MudText Typo="Typo.h5">@DilServisi.T("pdfGosterici.dosyaYok", "PDF dosyası bulunamadı.")</MudText>
            </div>
        }
        else if (_gorselMi)
        {
            <div class="orpay-pdf-gosterici-gorsel">
                <img src="@_belgeUrl" alt="@_baslik" />
            </div>
        }
        else
        {
            <div class="orpay-pdf-gosterici-cerceve" style="width:100%; height:85vh;">
                <MudPdfViewer Url="@_belgeUrl" />
            </div>
        }
    </section>
</main>
